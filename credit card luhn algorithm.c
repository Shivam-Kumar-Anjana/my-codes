#include<stdio.h>
#include<cs50.h>

int main()
{
    long n=0, card[n], i=0;
//luhn algorith

   //printf("enter the card number");

    for(i=0;i<=n;i++)
   {
    card[i]= get_long("card number:");
    //printf("%ld", card[i]);
   }
   //for( i=0;i<=n;i++)
   //{
      int k = card[i];
      printf("%d",k);
   //}


}
